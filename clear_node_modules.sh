#!/bin/bash 

rootpath="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
cd "$rootpath"
rm -fr ./node_modules

cd "$rootpath/app"
rm -fr ./node_modules

cd "$rootpath/service"
rm -fr ./node_modules